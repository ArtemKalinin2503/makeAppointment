import React from "react";
import { StyledWrapperHeader } from "./header.styled";

const Header: React.FC = () => {
  return (
    <StyledWrapperHeader>
      Header
    </StyledWrapperHeader>
  );
};

export default Header;
